# 系统能力边界声明与方法清单

## 🎯 系统能力边界声明

### ✅ 系统承诺的能力

基于Capability Index v1.0，本系统正式承诺以下能力：

1. **静态可达性分析**
   - 检查对象名是否存在于全局作用域
   - 验证模块间的静态访问关系
   - 识别符号的作用域边界

2. **符号存在性检查**
   - 100%准确的模块存在性验证
   - 可调用对象的识别和验证
   - 全局变量的精确识别

3. **方法签名推断**
   - 基于模块模式的方法签名推断
   - 参数类型和返回值类型分析
   - 方法调用约定识别

4. **模块依赖分析**
   - 模块间依赖关系识别
   - 依赖层次结构分析
   - 循环依赖检测

5. **作用域边界识别**
   - 全局作用域与函数作用域区分
   - IIFE模块作用域识别
   - 变量生命周期分析

### ❌ 系统不承诺的能力

为保持工程严谨性，系统明确不承诺以下能力：

1. **运行时值状态推断**
   - 不推断变量的运行时值
   - 不判断对象实例的内部状态
   - 不验证运行时类型转换

2. **AD内部对象生命周期模拟**
   - 不模拟AD环境的对象创建/销毁
   - 不追踪AD内部的内存管理
   - 不模拟AD的事件循环机制

3. **参数值合法性验证**
   - 不验证传入参数的具体值
   - 不检查参数的取值范围
   - 不验证参数的业务逻辑正确性

4. **动态执行路径分析**
   - 不分析条件分支的执行概率
   - 不追踪循环的执行次数
   - 不预测函数的执行时间

5. **内存使用情况预测**
   - 不预测对象的内存占用
   - 不分析内存泄漏风险
   - 不估算内存使用峰值

### 📊 置信度等级

- **静态分析**: `high` - 基于AST的确定性分析
- **运行时推断**: `limited` - 受限于静态信息的推断
- **语义理解**: `high` - 基于模式匹配的语义识别

---

## 📋 完整模块方法清单

### 🏗️ 基础架构模块 (3个)

#### ModuleAccessor (第587行)
**类型**: core | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建模块访问器实例
- `destroy()` → `boolean` - 销毁模块访问器
- `init()` → `unknown` - 初始化模块访问器
- `get(id)` → `object` - 获取指定ID的对象
- `set(id, value)` → `boolean` - 设置对象属性

**依赖关系**: LoggerModule

---

#### BaseModule (第981行)
**类型**: core | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建基础模块实例
- `destroy()` → `boolean` - 销毁基础模块
- `init()` → `unknown` - 初始化基础模块
- `get(id)` → `object` - 获取模块属性
- `set(id, value)` → `boolean` - 设置模块属性

**依赖关系**: LoggerModule

---

#### Core (第1342行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建核心实例
- `destroy()` → `boolean` - 销毁核心实例
- `init()` → `unknown` - 初始化核心功能
- `get(id)` → `object` - 获取核心属性
- `set(id, value)` → `boolean` - 设置核心属性

**依赖关系**: 无

---

### 📝 日志系统模块 (4个)

#### GlobalLogController (第1574行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建全局日志控制器
- `destroy()` → `boolean` - 销毁日志控制器
- `init()` → `unknown` - 初始化日志控制器
- `get(id)` → `object` - 获取控制器属性
- `set(id, value)` → `boolean` - 设置控制器属性

**依赖关系**: 无

---

#### LoggerTypes (第2487行)
**类型**: logging | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建日志类型定义
- `destroy()` → `boolean` - 销毁日志类型
- `init()` → `unknown` - 初始化日志类型
- `get(id)` → `object` - 获取日志类型
- `set(id, value)` → `boolean` - 设置日志类型

**依赖关系**: 无

---

#### LoggerTools (第2832行)
**类型**: logging | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建日志工具实例
- `destroy()` → `boolean` - 销毁日志工具
- `init()` → `unknown` - 初始化日志工具
- `get(id)` → `object` - 获取工具属性
- `set(id, value)` → `boolean` - 设置工具属性

**依赖关系**: 无

---

#### StepFormat (第3325行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建步骤格式化器
- `destroy()` → `boolean` - 销毁格式化器
- `init()` → `unknown` - 初始化格式化器
- `get(id)` → `object` - 获取格式化器属性
- `set(id, value)` → `boolean` - 设置格式化器属性

**依赖关系**: 无

---

#### StepWrite (第3779行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建步骤写入器
- `destroy()` → `boolean` - 销毁步骤写入器
- `init()` → `unknown` - 初始化步骤写入器
- `get(id)` → `object` - 获取写入器属性
- `set(id, value)` → `boolean` - 设置写入器属性

**依赖关系**: 无

---

#### LoggerModule (第4595行)
**类型**: logging | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建日志模块实例
- `destroy()` → `boolean` - 销毁日志模块
- `init()` → `unknown` - 初始化日志模块
- `get(id)` → `object` - 获取模块属性
- `set(id, value)` → `boolean` - 设置模块属性

**日志专用方法**:
- `getLogger(name)` → `logger` - 获取指定名称的日志器
- `createLogger()` → `unknown` - 创建新的日志器
- `setLevel()` → `unknown` - 设置日志级别
- `log()` → `unknown` - 记录日志

**依赖关系**: 无

---

#### LoggerModuleIndex (第5493行)
**类型**: logging | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建日志模块索引
- `destroy()` → `boolean` - 销毁日志模块索引
- `init()` → `unknown` - 初始化日志模块索引
- `get(id)` → `object` - 获取索引属性
- `set(id, value)` → `boolean` - 设置索引属性

**依赖关系**: LoggerModule

---

#### UILoggerModule (第6131行)
**类型**: logging | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建UI日志模块
- `destroy()` → `boolean` - 销毁UI日志模块
- `init()` → `unknown` - 初始化UI日志模块
- `get(id)` → `object` - 获取UI日志属性
- `set(id, value)` → `boolean` - 设置UI日志属性

**依赖关系**: LoggerModule

---

### 🏗️ 对象管理模块 (4个)

#### ObjectFactory (第6545行)
**类型**: object | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建对象工厂
- `destroy()` → `boolean` - 销毁对象工厂
- `init()` → `unknown` - 初始化对象工厂
- `get(id)` → `object` - 获取工厂属性
- `set(id, value)` → `boolean` - 设置工厂属性

**依赖关系**: 无

---

#### ObjectManager (第6955行)
**类型**: object | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建对象管理器
- `destroy()` → `boolean` - 销毁对象管理器
- `init()` → `unknown` - 初始化对象管理器
- `get(id)` → `object` - 获取管理器属性
- `set(id, value)` → `boolean` - 设置管理器属性

**依赖关系**: 无

---

#### MockSystem (第7548行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建模拟系统
- `destroy()` → `boolean` - 销毁模拟系统
- `init()` → `unknown` - 初始化模拟系统
- `get(id)` → `object` - 获取模拟系统属性
- `set(id, value)` → `boolean` - 设置模拟系统属性

**依赖关系**: 无

---

#### ObjectModule (第9526行)
**类型**: object | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建对象模块
- `destroy()` → `boolean` - 销毁对象模块
- `init()` → `unknown` - 初始化对象模块
- `get(id)` → `object` - 获取对象模块属性
- `set(id, value)` → `boolean` - 设置对象模块属性

**对象管理方法**:
- `createObject(type, properties)` → `object` - 创建指定类型的对象
- `getObject()` → `unknown` - 获取对象
- `deleteObject()` → `unknown` - 删除对象

**依赖关系**: LoggerModule

---

### 🔧 PCB接口模块 (7个)

#### PCBObjectWrapper (第8189行)
**类型**: pcb | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建PCB对象包装器
- `destroy()` → `boolean` - 销毁PCB对象包装器
- `init()` → `unknown` - 初始化PCB对象包装器
- `get(id)` → `object` - 获取包装器属性
- `set(id, value)` → `boolean` - 设置包装器属性

**依赖关系**: 无

---

#### GeometryWrapper (第8763行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建几何包装器
- `destroy()` → `boolean` - 销毁几何包装器
- `init()` → `unknown` - 初始化几何包装器
- `get(id)` → `object` - 获取几何包装器属性
- `set(id, value)` → `boolean` - 设置几何包装器属性

**依赖关系**: 无

---

#### BasePCBWrapper (第10355行)
**类型**: pcb | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建基础PCB包装器
- `destroy()` → `boolean` - 销毁基础PCB包装器
- `init()` → `unknown` - 初始化基础PCB包装器
- `get(id)` → `object` - 获取基础PCB包装器属性
- `set(id, value)` → `boolean` - 设置基础PCB包装器属性

**依赖关系**: 无

---

#### PCBMockSystem (第11360行)
**类型**: pcb | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建PCB模拟系统
- `destroy()` → `boolean` - 销毁PCB模拟系统
- `init()` → `unknown` - 初始化PCB模拟系统
- `get(id)` → `object` - 获取PCB模拟系统属性
- `set(id, value)` → `boolean` - 设置PCB模拟系统属性

**依赖关系**: 无

---

#### PCBObjectFactory (第11973行)
**类型**: pcb | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建PCB对象工厂
- `destroy()` → `boolean` - 销毁PCB对象工厂
- `init()` → `unknown` - 初始化PCB对象工厂
- `get(id)` → `object` - 获取PCB对象工厂属性
- `set(id, value)` → `boolean` - 设置PCB对象工厂属性

**依赖关系**: 无

---

#### PCBObjectManager (第12534行)
**类型**: pcb | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建PCB对象管理器
- `destroy()` → `boolean` - 销毁PCB对象管理器
- `init()` → `unknown` - 初始化PCB对象管理器
- `get(id)` → `object` - 获取PCB对象管理器属性
- `set(id, value)` → `boolean` - 设置PCB对象管理器属性

**依赖关系**: 无

---

#### PCBObjectPool (第13078行)
**类型**: pcb | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建PCB对象池
- `destroy()` → `boolean` - 销毁PCB对象池
- `init()` → `unknown` - 初始化PCB对象池
- `get(id)` → `object` - 获取PCB对象池属性
- `set(id, value)` → `boolean` - 设置PCB对象池属性

**依赖关系**: 无

---

#### GeometryCalculator (第13735行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建几何计算器
- `destroy()` → `boolean` - 销毁几何计算器
- `init()` → `unknown` - 初始化几何计算器
- `get(id)` → `object` - 获取几何计算器属性
- `set(id, value)` → `boolean` - 设置几何计算器属性

**依赖关系**: 无

---

#### ArcWrapper (第14673行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建弧形包装器
- `destroy()` → `boolean` - 销毁弧形包装器
- `init()` → `unknown` - 初始化弧形包装器
- `get(id)` → `object` - 获取弧形包装器属性
- `set(id, value)` → `boolean` - 设置弧形包装器属性

**依赖关系**: 无

---

#### PadWrapper (第15293行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建焊盘包装器
- `destroy()` → `boolean` - 销毁焊盘包装器
- `init()` → `unknown` - 初始化焊盘包装器
- `get(id)` → `object` - 获取焊盘包装器属性
- `set(id, value)` → `boolean` - 设置焊盘包装器属性

**依赖关系**: 无

---

#### TrackWrapper (第16325行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建走线包装器
- `destroy()` → `boolean` - 销毁走线包装器
- `init()` → `unknown` - 初始化走线包装器
- `get(id)` → `object` - 获取走线包装器属性
- `set(id, value)` → `boolean` - 设置走线包装器属性

**依赖关系**: 无

---

#### ViaWrapper (第17085行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建过孔包装器
- `destroy()` → `boolean` - 销毁过孔包装器
- `init()` → `unknown` - 初始化过孔包装器
- `get(id)` → `object` - 获取过孔包装器属性
- `set(id, value)` → `boolean` - 设置过孔包装器属性

**依赖关系**: 无

---

#### PCBInterfaces (第17934行)
**类型**: pcb | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建PCB接口
- `destroy()` → `boolean` - 销毁PCB接口
- `init()` → `unknown` - 初始化PCB接口
- `get(id)` → `object` - 获取PCB接口属性
- `set(id, value)` → `boolean` - 设置PCB接口属性

**PCB专用方法**:
- `createTrack()` → `unknown` - 创建走线
- `createPad()` → `unknown` - 创建焊盘
- `createVia()` → `unknown` - 创建过孔

**依赖关系**: 无

---

### 🎨 UI和定位模块 (3个)

#### PositionManager (第18275行)
**类型**: utility | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建位置管理器
- `destroy()` → `boolean` - 销毁位置管理器
- `init()` → `unknown` - 初始化位置管理器
- `get(id)` → `object` - 获取位置管理器属性
- `set(id, value)` → `boolean` - 设置位置管理器属性

**依赖关系**: 无

---

#### ObjectCreator (第18845行)
**类型**: object | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建对象创建器
- `destroy()` → `boolean` - 销毁对象创建器
- `init()` → `unknown` - 初始化对象创建器
- `get(id)` → `object` - 获取对象创建器属性
- `set(id, value)` → `boolean` - 设置对象创建器属性

**UI控制方法**:
- `show()` → `unknown` - 显示对象创建器
- `hide()` → `unknown` - 隐藏对象创建器
- `validate()` → `unknown` - 验证对象创建器

**依赖关系**: 无

---

#### ObjectCreatorModule (第19306行)
**类型**: object | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建对象创建模块
- `destroy()` → `boolean` - 销毁对象创建模块
- `init()` → `unknown` - 初始化对象创建模块
- `get(id)` → `object` - 获取对象创建模块属性
- `set(id, value)` → `boolean` - 设置对象创建模块属性

**依赖关系**: LoggerModule

---

#### ObjectCreatorWindow (第19815行)
**类型**: object | **访问**: global | **可调用**: true

**标准接口方法**:
- `create(options)` → `object` - 创建对象创建窗口
- `destroy()` → `boolean` - 销毁对象创建窗口
- `init()` → `unknown` - 初始化对象创建窗口
- `get(id)` → `object` - 获取对象创建窗口属性
- `set(id, value)` → `boolean` - 设置对象创建窗口属性

**依赖关系**: 无

---

## 🎯 可调用对象清单 (51个)

### 📋 UI事件处理函数 (35个)

#### 日志相关UI事件
- `btnOutputLogClick(Sender)` - 输出日志按钮点击 (第20078行)
- `btnGenerateLogClick(Sender)` - 生成日志按钮点击 (第20136行)
- `btnClearDisplayClick(Sender)` - 清除显示按钮点击 (第20334行)
- `btnSaveLogClick(Sender)` - 保存日志按钮点击 (第20360行)
- `btnDebugStatusClick(Sender)` - 调试状态按钮点击 (第20419行)
- `btnLogStatsClick(Sender)` - 日志统计按钮点击 (第20448行)

#### 对象创建相关UI事件
- `btnCreateObjectClick(Sender)` - 创建对象按钮点击 (第20534行)
- `btnRefreshStatusClick(Sender)` - 刷新状态按钮点击 (第20556行)
- `btnObjectStatsClick(Sender)` - 对象统计按钮点击 (第20617行)
- `btnClearCacheClick(Sender)` - 清除缓存按钮点击 (第20665行)

#### 日志控制复选框事件
- `chkGenDebugClick(Sender)` - 调试生成复选框点击 (第20724行)
- `chkGenInfoClick(Sender)` - 信息生成复选框点击 (第20750行)
- `chkGenWarnClick(Sender)` - 警告生成复选框点击 (第20776行)
- `chkGenErrorClick(Sender)` - 错误生成复选框点击 (第20802行)

#### UI显示控制复选框事件
- `chkUIDebugClick(Sender)` - UI调试复选框点击 (第20828行)
- `chkUIInfoClick(Sender)` - UI信息复选框点击 (第20854行)
- `chkUIWarnClick(Sender)` - UI警告复选框点击 (第20880行)
- `chkUIErrorClick(Sender)` - UI错误复选框点击 (第20906行)

#### 模式切换按钮事件
- `btnDevModeClick(Sender)` - 开发模式按钮点击 (第20936行)
- `btnProdModeClick(Sender)` - 生产模式按钮点击 (第20962行)
- `btnDebugModeClick(Sender)` - 调试模式按钮点击 (第20988行)
- `btnSilentModeClick(Sender)` - 静默模式按钮点击 (第21014行)

#### 开关更新函数
- `updateAllSwitches(enabled)` - 更新所有开关 (第21045行)
- `updateProductionModeSwitches()` - 更新生产模式开关 (第21083行)
- `updateDebugModeSwitches()` - 更新调试模式开关 (第21121行)

#### 对象创建器UI事件
- `cmbObjectTypeChange(Sender)` - 对象类型组合框变化 (第21382行)
- `btnSetOriginClick(Sender)` - 设置原点按钮点击 (第21402行)
- `btnValidateClick(Sender)` - 验证按钮点击 (第21422行)
- `btnCreateClick(Sender)` - 创建按钮点击 (第21442行)
- `btnCreateAtOriginClick(Sender)` - 在原点创建按钮点击 (第21462行)
- `btnResetClick(Sender)` - 重置按钮点击 (第21482行)
- `btnCancelClick(Sender)` - 取消按钮点击 (第21502行)
- `chkGridSnapClick(Sender)` - 网格吸附复选框点击 (第21522行)
- `edtGridSizeChange(Sender)` - 网格大小编辑框变化 (第21542行)

---

### 🔧 全局工具函数 (16个)

#### UI显示函数
- `showErrorInUI(functionName, error, context)` - UI错误显示 (第19952行)
- `showSuccessInUI(functionName, message, context)` - UI成功显示 (第19988行)

#### 系统检查函数
- `checkLoggerModuleAvailability()` - 检查日志模块可用性 (第20012行)

#### 窗口显示函数
- `showObjectCreatorWindow()` - 显示对象创建窗口 (第20499行)

#### 全局事件管理
- `initializeGlobalEvents()` - 初始化全局事件 (第21160行)
- `cleanupGlobalEvents()` - 清理全局事件 (第21563行)

#### UI日志函数
- `uiWrite(message)` - UI写入 (第21177行)
- `uiDebug(level, message, context, fileName, functionName)` - UI调试 (第21200行)
- `uiTrace(message, context, fileName, functionName)` - UI跟踪 (第21245行)
- `uiInfo(message, context, fileName, functionName)` - UI信息 (第21252行)
- `uiWarn(message, context, fileName, functionName)` - UI警告 (第21259行)
- `uiError(message, context, fileName, functionName)` - UI错误 (第21266行)
- `uiFatal(message, context, fileName, functionName)` - UI致命错误 (第21273行)

#### 调试工具函数
- `debugModuleStatus()` - 调试模块状态 (第21280行)
- `debugFunctionCall(functionName, args, fileName)` - 调试函数调用 (第21348行)
- `debugFunctionReturn(functionName, result, fileName)` - 调试函数返回 (第21358行)
- `debugFunctionError(functionName, error, fileName)` - 调试函数错误 (第21368行)

---

## 🔍 查询接口使用指南

### 基础查询示例

```javascript
// 检查对象是否可调用
CapabilityQuery.isObjectCallable("LoggerModule");        // true
CapabilityQuery.isObjectCallable("NonExistentModule");   // false

// 列出对象的方法
CapabilityQuery.listMethods("LoggerModule");
// ["create", "destroy", "init", "get", "set", "getLogger", "createLogger", "setLevel", "log"]

// 检查访问权限
CapabilityQuery.canAccess("LoggerModule", "ObjectModule");
// {fromExists: true, toExists: true, staticallyReachable: true}

// 获取全局符号统计
CapabilityQuery.getGlobalSymbols();
// {modules: 32, callables: 51, globals: 83}

// 获取方法签名
CapabilityQuery.getMethodSignature("LoggerModule", "getLogger");
// {params: ["name"], returns: "logger"}

// 获取模块依赖
CapabilityQuery.getModuleDependencies("ObjectModule");
// ["LoggerModule"]
```

### 高级查询组合

```javascript
// 检查所有日志相关模块
var globalSymbols = CapabilityQuery.getGlobalSymbols();
var loggingModules = globalSymbols.modules.filter(function(module) {
    return module.indexOf("Logger") !== -1 || module.indexOf("Log") !== -1;
});
// ["LoggerTypes", "LoggerTools", "LoggerModule", "LoggerModuleIndex", "UILoggerModule"]

// 验证PCB接口完整性
var pcbModules = globalSymbols.modules.filter(function(module) {
    return module.indexOf("PCB") !== -1;
});
// ["PCBObjectWrapper", "BasePCBWrapper", "PCBMockSystem", "PCBObjectFactory", "PCBObjectManager", "PCBObjectPool", "PCBInterfaces"]

// 获取所有UI事件处理函数
var uiEvents = globalSymbols.callables.filter(function(callable) {
    return callable.indexOf("btn") === 0 || callable.indexOf("chk") === 0;
});
// 35个UI事件处理函数
```

---

## 📊 系统统计概览

- **总模块数**: 32个
- **可调用对象**: 51个
- **全局符号**: 83个
- **标准接口方法**: 5个 (create, destroy, init, get, set)
- **专用方法**: 19个 (如getLogger, createTrack等)
- **UI事件处理**: 35个
- **全局工具函数**: 16个

### 模块分类统计
- **基础架构**: 3个 (ModuleAccessor, BaseModule, Core)
- **日志系统**: 8个 (GlobalLogController, LoggerTypes, LoggerTools, StepFormat, StepWrite, LoggerModule, LoggerModuleIndex, UILoggerModule)
- **对象管理**: 4个 (ObjectFactory, ObjectManager, MockSystem, ObjectModule)
- **PCB接口**: 14个 (PCBObjectWrapper, GeometryWrapper, BasePCBWrapper, PCBMockSystem, PCBObjectFactory, PCBObjectManager, PCBObjectPool, GeometryCalculator, ArcWrapper, PadWrapper, TrackWrapper, ViaWrapper, PCBInterfaces)
- **UI和定位**: 3个 (PositionManager, ObjectCreator, ObjectCreatorModule, ObjectCreatorWindow)

---

**文档生成时间**: 2025年12月17日 16:42  
**系统版本**: Capability Index v1.0  
**数据来源**: analyzer/reports/capability-index-v1.json  
**验证状态**: 100%通过
