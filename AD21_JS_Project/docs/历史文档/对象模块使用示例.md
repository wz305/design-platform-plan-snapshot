# ObjectModule 使用示例

本文档提供了 ObjectModule 的详细使用示例，帮助开发者快速上手和使用新模块。

## 目录
1. [基础使用](#基础使用)
2. [对象创建](#对象创建)
3. [批量操作](#批量操作)
4. [对象管理](#对象管理)
5. [几何计算](#几何计算)
6. [Mock 系统](#mock-系统)
7. [高级用法](#高级用法)
8. [错误处理](#错误处理)

## 基础使用

### 1. 模块初始化

```javascript
// 基础初始化
ObjectModule.initialize();

// 带配置的初始化
ObjectModule.initialize({
    enableLogging: true,           // 启用日志
    enableAutoRegistration: true,   // 自动注册到管理器
    enablePositionManagement: true,  // 启用位置管理
    enableGeometryCalculation: true, // 启用几何计算
    defaultLayer: "TopLayer",       // 默认层
    conflictThreshold: 10           // 冲突检测阈值
});

// 检查初始化状态
if (ObjectModule.isInitialized()) {
    console.log("ObjectModule 已初始化");
}
```

### 2. 获取模块信息

```javascript
// 获取支持的对象类型
var supportedTypes = ObjectModule.getSupportedTypes();
console.log("支持的对象类型:", supportedTypes);
// 输出: ["Arc", "Pad", "Track", "Via", "Board", ...]

// 检查特定类型是否支持
if (ObjectModule.isTypeSupported("Track")) {
    console.log("支持 Track 对象");
}

// 获取模块配置
var config = ObjectModule.getConfiguration();
console.log("当前配置:", config);

// 获取统计信息
var stats = ObjectModule.getStatistics();
console.log("模块统计:", stats);
```

## 对象创建

### 1. 创建 Mock 对象

```javascript
// 创建简单的 Track 对象
var track = ObjectModule.createMock("Track", {
    startX: 0,
    startY: 0,
    endX: 100,
    endY: 0,
    width: 0.1,
    layer: "TopLayer"
});

console.log("Track 对象:", track);
console.log("对象类型:", track.getObjectType()); // "Track"
console.log("是否为 Mock:", track.isMock());     // true
console.log("对象 ID:", track.getObjectId());
```

### 2. 创建不同类型的对象

```javascript
// 创建 Pad 对象
var pad = ObjectModule.createMock("Pad", {
    x: 50,
    y: 25,
    size: 1.0,
    layer: "TopLayer"
});

// 创建 Via 对象
var via = ObjectModule.createMock("Via", {
    x: 75,
    y: 25,
    size: 0.8,
    layers: ["TopLayer", "BottomLayer"]
});

// 创建 Arc 对象
var arc = ObjectModule.createMock("Arc", {
    centerX: 25,
    centerY: 25,
    radius: 10,
    startAngle: 0,
    endAngle: 90,
    layer: "TopLayer"
});
```

### 3. 从原生对象创建

```javascript
// 假设有一个原生 PCB 对象
var nativePCBObject = GetPCBObjectFromAD(); // 从 AD 获取的原生对象

// 创建封装器
var wrapper = ObjectModule.createObject(nativePCBObject, {
    objectType: "Track",
    enableCache: true
});
```

## 批量操作

### 1. 批量创建对象

```javascript
// 批量创建多个对象
var batchResult = ObjectModule.createBatch([
    {
        objectType: "Track",
        mockData: {
            startX: 0, startY: 0,
            endX: 50, endY: 0,
            width: 0.1
        }
    },
    {
        objectType: "Pad",
        mockData: {
            x: 25, y: 0,
            size: 1.0
        }
    },
    {
        objectType: "Via",
        mockData: {
            x: 50, y: 0,
            size: 0.8
        }
    }
]);

console.log("批量创建结果:");
console.log("- 成功:", batchResult.successCount);
console.log("- 失败:", batchResult.failedCount);
console.log("- 成功对象:", batchResult.success);
console.log("- 失败详情:", batchResult.failed);

// 处理创建的对象
for (var i = 0; i < batchResult.success.length; i++) {
    var obj = batchResult.success[i];
    console.log("对象 " + i + ":", obj.getObjectType());
}
```

### 2. 从迭代器批量创建

```javascript
// 假设有一个 PCB 对象迭代器
var iterator = GetPCBObjectIterator(); // 从 AD 获取的迭代器

// 批量创建 Track 对象
var tracks = ObjectModule.createFromIterator(iterator, "Track", {
    maxCount: 1000,        // 最大数量限制
    skipErrors: true       // 跳过错误继续处理
});

console.log("创建了 " + tracks.length + " 个 Track 对象");
```

## 对象管理

### 1. 获取和查询对象

```javascript
// 获取所有对象
var allObjects = ObjectModule.getAllObjects();
console.log("总对象数:", allObjects.length);

// 按类型获取对象
var tracks = ObjectModule.getObjectsByType("Track");
console.log("Track 对象数:", tracks.length);

// 按位置获取对象（在指定半径内）
var nearbyObjects = ObjectModule.getObjectsByPosition(
    {x: 25, y: 25},  // 中心点
    10                 // 搜索半径
);
console.log("附近对象数:", nearbyObjects.length);

// 获取特定对象
var specificObject = ObjectModule.getObject("Track_1234567890_1234");
if (specificObject) {
    console.log("找到对象:", specificObject.getObjectType());
}
```

### 2. 对象操作

```javascript
// 移除对象
var removed = ObjectModule.removeObject("Track_1234567890_1234");
if (removed) {
    console.log("对象已移除");
}

// 获取对象数量
var objectCount = ObjectModule.getObjectCount();
console.log("当前对象数:", objectCount);
```

## 几何计算

### 1. 基础几何计算

```javascript
// 计算两点间距离
var distance = ObjectModule.calculateDistance(
    {x: 0, y: 0},
    {x: 100, y: 0}
);
console.log("距离:", distance); // 100

// 创建几何计算器
var track = ObjectModule.createMock("Track", {
    startX: 0, startY: 0,
    endX: 100, endY: 100,
    width: 0.1
});

var geometry = ObjectModule.createGeometryCalculator(track);

// 获取对象中心点
var center = geometry.getCenter();
console.log("中心点:", center); // {x: 50, y: 50}

// 获取对象长度
var length = geometry.getLength();
console.log("长度:", length); // 141.421...

// 获取对象面积
var area = geometry.getArea();
console.log("面积:", area);
```

### 2. 高级几何计算

```javascript
// 计算两个对象间的距离
var track1 = ObjectModule.createMock("Track", {
    startX: 0, startY: 0,
    endX: 50, endY: 0,
    width: 0.1
});

var track2 = ObjectModule.createMock("Track", {
    startX: 100, startY: 0,
    endX: 150, endY: 0,
    width: 0.1
});

var geo1 = ObjectModule.createGeometryCalculator(track1);
var geo2 = ObjectModule.createGeometryCalculator(track2);

var distance = geo1.getDistanceTo(track2);
console.log("两轨道间距离:", distance);

// 检查对象相交
var intersects = geo1.intersects(track2);
console.log("是否相交:", intersects);

// 检查点是否在对象内
var containsPoint = geo1.containsPoint({x: 25, y: 0});
console.log("包含点 (25,0):", containsPoint);
```

### 3. 静态几何方法

```javascript
// 使用静态方法计算
var point1 = {x: 0, y: 0};
var point2 = {x: 100, y: 100};

// 计算距离
var distance = ObjectModule.GeometryWrapper.calculateDistance(point1, point2);

// 计算点到直线距离
var lineStart = {x: 0, y: 0};
var lineEnd = {x: 100, y: 0};
var point = {x: 50, y: 25};

var pointToLineDistance = ObjectModule.GeometryWrapper.calculatePointToLineDistance(
    point, lineStart, lineEnd
);

// 计算两直线交点
var line1 = {start: {x: 0, y: 0}, end: {x: 100, y: 100}};
var line2 = {start: {x: 0, y: 100}, end: {x: 100, y: 0}};

var intersection = ObjectModule.GeometryWrapper.calculateLineIntersection(line1, line2);
console.log("交点:", intersection); // {x: 50, y: 50}
```

## Mock 系统

### 1. 注册 Mock 模板

```javascript
// 注册自定义 Track 模板
ObjectModule.MockSystem.registerMockTemplate("StandardTrack", "Track", {
    defaultProperties: {
        width: 0.1,
        layer: "TopLayer"
    },
    validationRules: {
        width: {min: 0.01, max: 1.0},
        layer: {required: true}
    }
});

// 注册自定义 Pad 模板
ObjectModule.MockSystem.registerMockTemplate("StandardPad", "Pad", {
    defaultProperties: {
        size: 1.0,
        layer: "TopLayer",
        shape: "Round"
    },
    validationRules: {
        size: {min: 0.1, max: 5.0},
        shape: {enum: ["Round", "Square", "Rectangle"]}
    }
});
```

### 2. 使用 Mock 模板

```javascript
// 使用模板创建对象
var track = ObjectModule.createMock("Track", {
    startX: 0, startY: 0,
    endX: 100, endY: 0
    // width 和 layer 将使用模板默认值
});

var pad = ObjectModule.createMock("Pad", {
    x: 50, y: 25
    // size, layer, shape 将使用模板默认值
});
```

### 3. Mock 系统配置

```javascript
// 配置 Mock 系统
ObjectModule.MockSystem.configure({
    enableAutoId: true,        // 自动生成 ID
    enableTimestamp: true,     // 添加时间戳
    defaultLayer: "TopLayer",  // 默认层
    defaultUnit: "mil"         // 默认单位
});

// 获取 Mock 统计信息
var mockStats = ObjectModule.MockSystem.getMockStatistics();
console.log("Mock 统计:", mockStats);
```

## 高级用法

### 1. 自定义封装器

```javascript
// 注册自定义封装器
function CustomTrackWrapper(objectData) {
    this._objectData = objectData;
    this._cache = {};
}

CustomTrackWrapper.prototype.getObjectType = function() {
    return "Track";
};

CustomTrackWrapper.prototype.getObjectId = function() {
    return this._objectData.objectId;
};

CustomTrackWrapper.prototype.getCustomProperty = function() {
    return this._objectData.customProperty || "default";
};

// 注册封装器
ObjectModule.ObjectFactory.registerWrapperConstructor("CustomTrack", CustomTrackWrapper);
```

### 2. 事件监听

```javascript
// 监听对象创建事件
ObjectModule.ObjectManager.addEventListener("objectCreated", function(event) {
    console.log("新对象创建:", event.objectId, event.objectType);
});

// 监听对象移除事件
ObjectModule.ObjectManager.addEventListener("objectRemoved", function(event) {
    console.log("对象移除:", event.objectId);
});
```

### 3. 性能优化

```javascript
// 配置缓存
ObjectModule.configure({
    enableLogging: false,  // 关闭日志提高性能
    enableAutoRegistration: true
});

// 配置几何计算缓存
ObjectModule.GeometryWrapper.configure({
    precision: 3,           // 降低精度提高性能
    enableCache: true,       // 启用缓存
    cacheTimeout: 5000      // 缓存超时
});

// 批量操作时临时关闭日志
var originalConfig = ObjectModule.getConfiguration();
ObjectModule.configure({enableLogging: false});

// 执行批量操作...
var batchResult = ObjectModule.createBatch(largeObjectList);

// 恢复配置
ObjectModule.configure(originalConfig);
```

## 错误处理

### 1. 基础错误处理

```javascript
try {
    var track = ObjectModule.createMock("Track", {
        startX: 0, startY: 0,
        endX: 100, endY: 0,
        width: 0.1
    });
} catch (error) {
    console.error("创建 Track 失败:", error.message);
    // 处理错误...
}
```

### 2. 批量操作错误处理

```javascript
var batchResult = ObjectModule.createBatch([
    {objectType: "Track", mockData: {...}},
    {objectType: "InvalidType", mockData: {...}},  // 会失败
    {objectType: "Pad", mockData: {...}}
]);

if (batchResult.failedCount > 0) {
    console.log("批量创建中有失败项:");
    for (var i = 0; i < batchResult.failed.length; i++) {
        var failure = batchResult.failed[i];
        console.log("- 索引 " + failure.index + ": " + failure.error);
    }
}
```

### 3. 验证错误处理

```javascript
// 检查模块状态
if (!ObjectModule.isInitialized()) {
    throw new Error("ObjectModule 未初始化");
}

// 检查对象类型支持
if (!ObjectModule.isTypeSupported("CustomType")) {
    throw new Error("不支持的对象类型: CustomType");
}

// 检查参数有效性
function validateTrackData(data) {
    if (!data || typeof data !== "object") {
        throw new Error("Track 数据必须是对象");
    }
    if (typeof data.startX !== "number" || typeof data.startY !== "number") {
        throw new Error("起点坐标必须是数字");
    }
    if (typeof data.endX !== "number" || typeof data.endY !== "number") {
        throw new Error("终点坐标必须是数字");
    }
    if (typeof data.width !== "number" || data.width <= 0) {
        throw new Error("宽度必须是正数");
    }
}
```

## 完整示例

### PCB 设计助手示例

```javascript
// PCB 设计助手类
function PCBDesignAssistant() {
    this.initialize();
}

PCBDesignAssistant.prototype.initialize = function() {
    // 初始化 ObjectModule
    ObjectModule.initialize({
        enableLogging: true,
        enableAutoRegistration: true,
        enablePositionManagement: true,
        enableGeometryCalculation: true,
        defaultLayer: "TopLayer",
        conflictThreshold: 5
    });
    
    // 注册常用模板
    this.registerTemplates();
};

PCBDesignAssistant.prototype.registerTemplates = function() {
    // 标准走线模板
    ObjectModule.MockSystem.registerMockTemplate("StandardTrack", "Track", {
        defaultProperties: {
            width: 0.1,
            layer: "TopLayer"
        }
    });
    
    // 标准焊盘模板
    ObjectModule.MockSystem.registerMockTemplate("StandardPad", "Pad", {
        defaultProperties: {
            size: 1.0,
            layer: "TopLayer"
        }
    });
};

PCBDesignAssistant.prototype.createSimpleBoard = function() {
    console.log("创建简单 PCB 板...");
    
    // 创建板框
    var board = ObjectModule.createMock("Board", {
        width: 100,
        height: 80,
        layer: "Mechanical"
    });
    
    // 创建连接器焊盘
    var connectors = ObjectModule.createBatch([
        {
            objectType: "Pad",
            mockData: {x: 10, y: 40, size: 1.5}
        },
        {
            objectType: "Pad", 
            mockData: {x: 10, y: 50, size: 1.5}
        },
        {
            objectType: "Pad",
            mockData: {x: 10, y: 60, size: 1.5}
        }
    ]);
    
    // 创建走线连接焊盘
    var tracks = ObjectModule.createBatch([
        {
            objectType: "Track",
            mockData: {
                startX: 10, startY: 40,
                endX: 50, endY: 40,
                width: 0.1
            }
        },
        {
            objectType: "Track",
            mockData: {
                startX: 10, startY: 50,
                endX: 50, endY: 50,
                width: 0.1
            }
        },
        {
            objectType: "Track",
            mockData: {
                startX: 10, startY: 60,
                endX: 50, endY: 60,
                width: 0.1
            }
        }
    ]);
    
    // 创建过孔
    var vias = ObjectModule.createBatch([
        {
            objectType: "Via",
            mockData: {x: 50, y: 40, size: 0.8}
        },
        {
            objectType: "Via",
            mockData: {x: 50, y: 50, size: 0.8}
        },
        {
            objectType: "Via",
            mockData: {x: 50, y: 60, size: 0.8}
        }
    ]);
    
    console.log("PCB 板创建完成!");
    this.printStatistics();
};

PCBDesignAssistant.prototype.printStatistics = function() {
    var stats = ObjectModule.getStatistics();
    
    console.log("\n=== PCB 设计统计 ===");
    console.log("总对象数:", ObjectModule.getObjectCount());
    console.log("创建的对象:", stats.module.statistics.objectsCreated);
    console.log("Mock 对象:", stats.module.statistics.mockObjectsCreated);
    console.log("几何计算:", stats.module.statistics.geometryCalculations);
    
    console.log("\n按类型统计:");
    var factoryStats = stats.factory.byType;
    for (var type in factoryStats) {
        if (factoryStats.hasOwnProperty(type)) {
            console.log("- " + type + ": " + factoryStats[type]);
        }
    }
};

PCBDesignAssistant.prototype.checkDesignRules = function() {
    console.log("\n=== 设计规则检查 ===");
    
    var allObjects = ObjectModule.getAllObjects();
    var conflicts = 0;
    
    // 检查对象间距
    for (var i = 0; i < allObjects.length; i++) {
        for (var j = i + 1; j < allObjects.length; j++) {
            var obj1 = allObjects[i];
            var obj2 = allObjects[j];
            
            var geo1 = ObjectModule.createGeometryCalculator(obj1);
            if (geo1.intersects(obj2)) {
                console.log("冲突发现:", obj1.getObjectId(), "<->", obj2.getObjectId());
                conflicts++;
            }
        }
    }
    
    console.log("设计规则检查完成，发现", conflicts, "个冲突");
    return conflicts === 0;
};

// 使用示例
var assistant = new PCBDesignAssistant();
assistant.createSimpleBoard();
var isValid = assistant.checkDesignRules();

if (isValid) {
    console.log("\n🎉 PCB 设计验证通过!");
} else {
    console.log("\n⚠️  PCB 设计存在问题，请检查冲突");
}
```

## 总结

ObjectModule 提供了强大而灵活的 PCB 对象管理能力，支持：

- ✅ **多种对象类型**: Track、Pad、Via、Arc、Board 等
- ✅ **批量操作**: 高效的批量创建和管理
- ✅ **几何计算**: 距离、面积、相交检测等
- ✅ **Mock 系统**: 支持离线开发和测试
- ✅ **性能优化**: 缓存机制和批量处理
- ✅ **错误处理**: 完善的错误处理和验证
- ✅ **扩展性**: 支持自定义封装器和模板

通过这些示例，您可以快速掌握 ObjectModule 的使用方法，并在实际项目中应用。
